@model IEnumerable<ShopWebMVC.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<h2>List Category</h2>

<p>
    <div><a class="btn btn-outline-primary" asp-action="Create">Create Category</a></div>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Created_At)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <img class="imgs" src="~/cateimg/@item.Image" height="50">
            </td>
            <td>
                @String.Format("{0:dd/MM/yyyy}",@item.Created_At)
            </td>
            <td>
                 @Html.ActionLink("Edit", "Edit", new {  id = item.Id } , new { @class = "btn btn-outline-warning mx-2"})
                 @Html.ActionLink("Details", "Details", new {  id = item.Id } , new { @class = "btn btn-outline-success mx-2"})
                 @Html.ActionLink("Delete", "Delete", new {  id=item.Id } , new { @class = "btn btn-outline-danger"})
            </td>
        </tr>
}
    </tbody>
</table>
